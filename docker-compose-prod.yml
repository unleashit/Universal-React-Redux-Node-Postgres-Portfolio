version: '3.4'

services:
  db:
    # image: jasongallagher_db
    image: registry.gitlab.com/unleashit/jasongallagher.org/db
  app:
    # image: jasongallagher:prod
    image: registry.gitlab.com/unleashit/jasongallagher.org/app
    environment:
    - NODE_ENV=production
  staging:
    # image: jasongallagher:staging
    image: registry.gitlab.com/unleashit/jasongallagher.org/staging
    environment:
      - NODE_ENV=production
