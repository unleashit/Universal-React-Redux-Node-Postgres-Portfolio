// import serverMiddleware from '../router';
// import ReactDOM from 'react-dom';
// import reactRouter from 'react-router';

// import ReactGA from 'react-ga';
// jest.mock('react-ga');
// jest.mock('react-dom');
// const matchMock = jest.spyOn(reactRouter, "match")
//     .mockImplementation((config, handler) => {
//
//     });
// const mockReactGAInitialize = jest.spyOn(ReactGA, "initialize")
//     .mockImplementation(jest.fn());

describe('router.js', () => {
    // const req = {
    //     url: '/',
    // };
    // const res = {
    //     send: jest.fn(),
    //     status: jest.fn(),
    // };
    // const next = jest.fn();

    it('server middleware', () => {
        expect(true).toBe(true);
    });
});
