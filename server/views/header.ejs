<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <% if (title === '') { %>
        <title>Jason Gallagher - Front End Engineer in the San Francisco Bay Area</title>
    <% } else { %>
        <title><%= title %> - Jason Gallagher</title>
    <% } %>
    <link rel="stylesheet" href="/css/bootstrap.min.css">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
<nav class="navbar navbar-inverse navbar-fixed-top">
    <div class="container">
        <div id="navbar" class="collapse navbar-collapse">
            <ul class="nav navbar-nav">
                <li class="<%= (activeClass === 'home') ? 'active' : '' %>"><a href="/">Home</a></li>
                <li class="<%= (activeClass === 'manage-portfolio') ? 'active' : '' %>"><a href="/admin/portfolio">Manage Portfolio</a></li>
                <li class="<%= (activeClass === 'users') ? 'active' : '' %>"><a href="/admin/users">Users</a></li>
            </ul>
            <ul class="nav navbar-nav navbar-right">
                <% if (!auth) { %>
                    <li><a href="/login">Log In</a></li>
                <% } else { %>
                    <li><a href="/logout">Log Out</a></li>
                <% } %>
            </ul>
        </div>
    </div>
</nav>
<div class="container">
    <div class="main-content">